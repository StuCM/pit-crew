schema_version: '1.1'
name: scout-finds-prior-fix-and-every-instance
description: 'The scout on its own: finds the earlier fix, the helper, every instance of the cause, and says what it could not reach.'
tags: [crew-scout, brownfield]
context:
  scaffold_script: fixture.sh
execution:
  max_turns: 15
  timeout_seconds: 420
  allowed_tools: [Read, Glob, Grep, Skill, Agent, 'Bash(git log:*)', 'Bash(git show:*)']
