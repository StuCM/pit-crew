schema_version: '1.1'
name: spec-answers-a-question
description: 'A question about how code works is answered, with no spec, no scout and no offer to plan.'
tags: [crew-spec, routing]
context:
  scaffold_script: fixture.sh
execution:
  max_turns: 15
  timeout_seconds: 420
  allowed_tools: [Read, Glob, Grep, Skill, Agent, 'Bash(git log:*)', 'Bash(git show:*)']
